### minecraft-netfilter-module

A simple linux kernel module to filter bad minecraft packets.

#### Build
`$ make`

Enable

```$ sudo insmod minecraft-filter.ko```

Disable:

```$ sudo rmmod minecraft-filter.ko```
